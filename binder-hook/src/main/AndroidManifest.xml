<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="asynctask.com.kc.binder_hook">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".MainActivity" android:label="003BinderHook">
            <intent-filter>
                <action android:name="com.kc.life.rise" />
            </intent-filter>
        </activity>


        <activity android:name=".intercept_activity.InterceptActivity" android:label="005Hook掉Activity">
            <intent-filter>
                <action android:name="com.kc.life.rise" />
            </intent-filter>
        </activity>

        <activity android:name=".intercept_activity.StubActivity">
        </activity>

    </application>

</manifest>
